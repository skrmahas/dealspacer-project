# PostgreSQL (required)
DATABASE_URL=postgresql://user:password@localhost:5432/bei

# Optional: directory for uploaded files and generated reports
# Must match the worker's DATA_DIR so both can read/write the same files
DATA_DIR=./data
