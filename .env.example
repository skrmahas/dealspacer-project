# PostgreSQL (required)
DATABASE_URL=postgresql://user:password@localhost:5432/bei

# OpenAI API key (required for GPT-4o extraction)
OPENAI_API_KEY=sk-...

# Optional: shared data directory for file storage
# Defaults to ./data in each package — set this so web + worker share the same files
DATA_DIR=./data
